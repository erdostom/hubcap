server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://loki:3100/loki/api/v1/push

scrape_configs:
  # ---------------------------------------------------------------------------
  # Rails applications — add one job per app
  # ---------------------------------------------------------------------------
  # Example: uncomment and duplicate for each Rails app's log path
  #
  # - job_name: "my-rails-app"
  #   static_configs:
  #     - targets: [localhost]
  #       labels:
  #         app: "my-rails-app"
  #         environment: "production"
  #         __path__: "/var/log/my-rails-app/*.log"
  #   pipeline_stages:
  #     - json:
  #         expressions:
  #           level: level
  #           method: method
  #           path: path
  #           status: status
  #           duration: duration
  #     - labels:
  #         level:
  #         method:
  #         status:
